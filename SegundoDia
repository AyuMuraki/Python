
# Aprendendo Python

Segundo dia de aula: Aprendendo os seguintes temas: interpolação de strings, textos de múltiplas linhas, arredondamento de números e estrutura de dados.



## Objetivo da Aula

O foco da aula de hoje foi aprofundar conceitos de Python, como interpolação de strings, arredondamento de números e o uso de textos de múltiplas linhas e estrutura de dados, essenciais para a manipulação de dados e formatação de saídas.


## Aprendizados

Durante a aula de hoje, aprendi:

Interpolação de Strings: Uma técnica que permite inserir variáveis diretamente dentro de uma string, facilitando a criação de mensagens dinâmicas. Exemplo:

Exemplo:

```nome = "Ayumi"
print(f"Olá, {nome}!")
```

Arredondamento de Números: Utilizando funções como round(), aprendi a controlar a precisão de números decimais, o que é muito útil em cálculos que precisam de resultados específicos.

Textos de Múltiplas Linhas: Para textos que precisam ser exibidos ou armazenados em mais de uma linha, utilizei três aspas (""") para criar strings de várias linhas, tornando o código mais organizado e legível.

Exemplo

```texto = """Este é um texto
de várias linhas
em Python."""
print(texto)
```


## Estrutura de Dados

As linguagens de programação utilizam estruturas de dados para **otimizar o armazenamento, 
a recuperação e a manipulação de informações**.

O Python possui vários **tipos de dado**s nativos da linguagem, os **tipos de dados mais básicos** são:

| Tipo de dado | Descrição |
| --- | --- |
| **str** | Valores do tipo texto |
| **int** | Números inteiros (1, 5, 10, ...) |
| **float** | Números de ponto flutuante (1.5, 0.99, 8.495, ...) |
| **bool** | Valores booleanos ou lógicos (Verdadeiro / Falso) |

Conversão entre tipos de dados
Existem funções que nos permitem converter de um tipo de dado para outro tipo, por exemplo: 
Converter um número do tipo string para o tipo inteiro.

 Exemplo:
```
n = '10'

n = int(n)
```
Conversão do tipo de dado string para o tipo número de ponto flutuante:

Exemplo:
```
texto = '5.99'

texto = float(texto)
```


Conversão do tipo de dado inteiro para o tipo string:

Exemplo:
```
n = 8

n = str(n)
```

Conversão do tipo de dado inteiro para o tipo booleano:
Exemplo:
```
n = 8

n = bool(n)
```

# Como verificar o tipo de dado

Existem várias maneiras de verificar o tipo de dados no Python. Uma das maneiras mais simples é utilizando as funções type() ou isinstance().
Função type()
A função type retorna o tipo de dado do valor passado como argumento.

Exemplo:
```
type(10)

type('python')
```


### Função **isinstance**()

A função **isinstance** faz um comparação entre o **valor do dado** e o **tipo de dado** informado e se os dois forem iguais a função retornará verdadeiro, senão retornará falso.



Exemplo:

```
isinstance(10, int)

isinstance(10, str)
```
💡A função **isinstance*** *recebe dois argumentos:

***isinstance**(**valor do dado**, **tipo de dado**)*

1. ***valor do dado**: Valor do dado que será verificado*
2. ***tipo de dado**: Tipo de dado que o **valor do dado** será verificado*

*Está função sempre retorna um valor **booleano** (Verdadeiro ou Falso).*



#

*Os maiores desafios foram adaptar-me ao uso desses conceitos em conjunto, mas a prática contínua está ajudando a solidificar o conhecimento. A cada exercício, a lógica fica mais clara.*
## Recursos e Dicas

Utilizamos o VS Code com o recurso de preenchimento automático de código (Code Completion), que facilita muito a escrita da sintaxe. Além disso, o Anaconda Cloud foi utilizado para executar os códigos, sendo uma ótima plataforma para rodar Jupyter Notebooks sem precisar instalar nada localmente.




## Próximos Passos

Estou animada para continuar explorando Python e ver como esses conceitos serão aplicados em projetos mais complexos no futuro

